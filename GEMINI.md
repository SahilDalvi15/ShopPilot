# Git Commit Rules

- **No Conventional Commits**: DO NOT use prefixes like `feat:`, `fix:`, or `docs:` in commit messages. 
- **Message Only**: Write only the descriptive message of what was changed (e.g., "Add advanced product filters", NOT "feat: add advanced product filters").

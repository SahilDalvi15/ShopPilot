# ShopPilot AI - Security Design

## Security Overview

**Security Philosophy**: Defense in Depth  
**Compliance**: OWASP Top 10, GDPR Ready  
**Security Layers**: Authentication, Authorization, Encryption, Validation, Monitoring

---

## Security Architecture

### Security Layers
```
┌─────────────────────────────────────┐
│     Network Security Layer          │
│  (HTTPS, SSL/TLS, Firewall)         │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│     Application Security Layer       │
│  (Rate Limiting, CORS, Helmet)      │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│     Authentication Layer            │
│  (JWT, Refresh Tokens, bcrypt)       │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│     Authorization Layer             │
│  (Role-Based Access Control)        │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│     Input Validation Layer          │
│  (Sanitization, Validation)         │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│     Data Security Layer              │
│  (Encryption, Hashing, Secrets)     │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│     Monitoring & Logging Layer       │
│  (Audit Logs, Error Tracking)       │
└─────────────────────────────────────┘
```

---

## Authentication Security

### JWT Token Strategy

#### Access Token
- **Algorithm**: HS256
- **Expiration**: 15 minutes
- **Storage**: Memory (frontend)
- **Payload**: User ID, role, token type
- **Secret**: Environment variable (JWT_SECRET)

#### Refresh Token
- **Algorithm**: HS256
- **Expiration**: 7 days
- **Storage**: httpOnly cookie (secure)
- **Payload**: User ID, token type
- **Secret**: Environment variable (JWT_REFRESH_SECRET)
- **Storage in Redis**: For revocation support

### Token Payload Structure
```javascript
// Access Token
{
  userId: "user_123",
  email: "user@example.com",
  role: "customer",
  type: "access",
  iat: 1623672000,
  exp: 1623672900
}

// Refresh Token
{
  userId: "user_123",
  type: "refresh",
  iat: 1623672000,
  exp: 1624276800
}
```

### Password Security
- **Hashing Algorithm**: bcrypt
- **Cost Factor**: 12 (recommended for 2026)
- **Salt**: Automatically generated by bcrypt
- **Password Requirements**:
  - Minimum 8 characters
  - Maximum 128 characters
  - At least one uppercase letter
  - At least one lowercase letter
  - At least one number
  - At least one special character (@$!%*?&)

### Authentication Flow Security
1. **Login**: Validate credentials, generate tokens
2. **Token Storage**: Access token in memory, refresh token in httpOnly cookie
3. **Token Refresh**: Automatic refresh before expiry
4. **Logout**: Invalidate refresh token in Redis
5. **Password Reset**: Time-limited reset token (1 hour expiry)

### Session Management
- **Session Storage**: Redis
- **Session Expiry**: 7 days (configurable)
- **Session Revocation**: Immediate on logout
- **Concurrent Sessions**: Limit to 3 active sessions per user

---

## Authorization Security

### Role-Based Access Control (RBAC)

#### User Roles
1. **Customer**: Basic shopping functionality
2. **Admin**: Product, order, inventory management
3. **Super Admin**: User management, system configuration

#### Permission Matrix

| Feature | Customer | Admin | Super Admin |
|---------|----------|-------|-------------|
| View Products | ✓ | ✓ | ✓ |
| Add to Cart | ✓ | ✓ | ✓ |
| Place Order | ✓ | ✓ | ✓ |
| View Orders | ✓ | ✓ | ✓ |
| Manage Profile | ✓ | ✓ | ✓ |
| Create Product | ✗ | ✓ | ✓ |
| Update Product | ✗ | ✓ | ✓ |
| Delete Product | ✗ | ✓ | ✓ |
| Manage Orders | ✗ | ✓ | ✓ |
| Manage Users | ✗ | ✗ | ✓ |
| Manage Admins | ✗ | ✗ | ✓ |
| System Settings | ✗ | ✗ | ✓ |

### Authorization Middleware
```javascript
// Role-based authorization
const authorize = (roles) => {
  return (req, res, next) => {
    if (!roles.includes(req.user.role)) {
      return res.status(403).json({
        success: false,
        message: 'Insufficient permissions'
      });
    }
    next();
  };
};

// Resource ownership check
const checkOwnership = (resourceType) => {
  return async (req, res, next) => {
    const resource = await ResourceModel.findById(req.params.id);
    if (!resource) {
      return res.status(404).json({
        success: false,
        message: 'Resource not found'
      });
    }
    if (resource.userId.toString() !== req.user.userId && req.user.role !== 'admin') {
      return res.status(403).json({
        success: false,
        message: 'Access denied'
      });
    }
    next();
  };
};
```

---

## Input Validation & Sanitization

### Validation Strategy
- **Server-side validation**: Mandatory for all inputs
- **Client-side validation**: UX enhancement only
- **Validation library**: express-validator
- **Sanitization**: Trim, escape, normalize

### SQL Injection Prevention
- **Parameterized queries**: Mongoose ODM handles this
- **Input sanitization**: Remove SQL patterns
- **ORM usage**: Never raw SQL queries

### XSS Prevention
- **Input sanitization**: Escape HTML entities
- **Output encoding**: Encode user-generated content
- **CSP headers**: Content-Security-Policy
- **DOMPurify**: For frontend HTML sanitization

### CSRF Protection
- **SameSite cookies**: httpOnly, secure, SameSite=strict
- **CSRF tokens**: For state-changing operations
- **Origin validation**: Check Referer/Origin headers

---

## Data Security

### Encryption at Rest
- **Database**: MongoDB Atlas encryption at rest
- **File Storage**: Cloudinary encryption
- **Environment Variables**: Never commit to git

### Encryption in Transit
- **HTTPS**: TLS 1.3
- **API Communication**: HTTPS only
- **Database Connection**: TLS enabled

### Sensitive Data Handling
- **Passwords**: bcrypt hashed, never stored plain
- **Payment Data**: Handled by Razorpay, never stored
- **PII**: Encrypted where possible
- **API Keys**: Environment variables only

### Data Masking
- **Email**: user***@example.com in logs
- **Phone**: +91******210 in logs
- **Credit Card**: Never stored
- **Tokens**: Never logged in full

---

## API Security

### Rate Limiting
- **Unauthenticated**: 100 requests per 15 minutes
- **Authenticated**: 1000 requests per 15 minutes
- **Auth endpoints**: 5 requests per 15 minutes per IP
- **Implementation**: express-rate-limit

### CORS Configuration
```javascript
const corsOptions = {
  origin: process.env.CORS_ORIGIN || 'http://localhost:5173',
  credentials: true,
  methods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH'],
  allowedHeaders: ['Content-Type', 'Authorization'],
  maxAge: 86400 // 24 hours
};
```

### API Key Security
- **Razorpay**: Environment variables
- **Cloudinary**: Environment variables
- **SMTP**: Environment variables
- **Never expose**: In client-side code or git

### API Versioning
- **URL-based**: `/api/v1/`
- **Backward compatibility**: Maintain old versions
- **Deprecation policy**: 6 months notice

---

## Webhook Security

### Razorpay Webhook
- **Signature verification**: HMAC SHA256
- **Secret**: Environment variable
- **Idempotency**: Handle duplicate webhooks
- **Timestamp validation**: Reject old webhooks

### Webhook Verification
```javascript
const crypto = require('crypto');

const verifyWebhookSignature = (payload, signature, secret) => {
  const expectedSignature = crypto
    .createHmac('sha256', secret)
    .update(payload)
    .digest('hex');
  
  return crypto.timingSafeEqual(
    Buffer.from(signature),
    Buffer.from(expectedSignature)
  );
};
```

---

## File Upload Security

### File Validation
- **Allowed types**: jpg, jpeg, png, webp
- **Max size**: 5MB per file
- **Max files**: 10 per upload
- **Virus scanning**: Cloudinary built-in

### File Storage
- **Storage**: Cloudinary (CDN)
- **Access**: Public URLs only
- **No execution**: Files stored as static assets
- **Metadata**: Strip EXIF data

### Upload Middleware
```javascript
const multer = require('multer');
const { CloudinaryStorage } = require('multer-storage-cloudinary');

const storage = new CloudinaryStorage({
  cloudinary: cloudinary,
  params: {
    folder: 'shoppilot',
    allowed_formats: ['jpg', 'jpeg', 'png', 'webp'],
    max_file_size: 5242880 // 5MB
  }
});

const upload = multer({
  storage: storage,
  limits: {
    fileSize: 5242880, // 5MB
    files: 10
  },
  fileFilter: (req, file, cb) => {
    const allowedTypes = ['image/jpeg', 'image/jpg', 'image/png', 'image/webp'];
    if (!allowedTypes.includes(file.mimetype)) {
      return cb(new Error('Invalid file type'), false);
    }
    cb(null, true);
  }
});
```

---

## Session & Cookie Security

### Cookie Configuration
```javascript
const cookieOptions = {
  httpOnly: true,
  secure: process.env.NODE_ENV === 'production',
  sameSite: 'strict',
  maxAge: 7 * 24 * 60 * 60 * 1000, // 7 days
  domain: process.env.COOKIE_DOMAIN || undefined
};
```

### Session Security
- **HttpOnly**: Prevent JavaScript access
- **Secure**: HTTPS only in production
- **SameSite**: Prevent CSRF
- **Expiry**: 7 days
- **Rotation**: Regenerate on privilege change

---

## Security Headers

### Helmet Configuration
```javascript
const helmet = require('helmet');

app.use(helmet({
  contentSecurityPolicy: {
    directives: {
      defaultSrc: ["'self'"],
      scriptSrc: ["'self'", "'unsafe-inline'", "https://checkout.razorpay.com"],
      styleSrc: ["'self'", "'unsafe-inline'"],
      imgSrc: ["'self'", "data:", "https://res.cloudinary.com"],
      connectSrc: ["'self'", "https://api.razorpay.com"],
      frameSrc: ["'self'", "https://checkout.razorpay.com"]
    }
  },
  hsts: {
    maxAge: 31536000,
    includeSubDomains: true,
    preload: true
  },
  noSniff: true,
  xssFilter: true,
  referrerPolicy: { policy: 'strict-origin-when-cross-origin' }
}));
```

### Security Headers
- **X-Content-Type-Options**: nosniff
- **X-Frame-Options**: DENY
- **X-XSS-Protection**: 1; mode=block
- **Strict-Transport-Security**: max-age=31536000
- **Content-Security-Policy**: Custom policy
- **Referrer-Policy**: strict-origin-when-cross-origin

---

## Error Handling Security

### Error Response Format
- **No stack traces** in production
- **Generic error messages** for users
- **Detailed errors** in logs only
- **Sanitized error data** before response

### Error Logging
- **Stack traces**: Logged server-side only
- **Sensitive data**: Never logged
- **PII**: Masked in logs
- **Error codes**: Standardized for client

---

## Audit Logging

### Audit Log Events
- User authentication (login, logout, failed attempts)
- Permission changes
- Data modifications
- Sensitive operations
- Admin actions

### Audit Log Structure
```javascript
{
  userId: ObjectId,
  action: String, // 'LOGIN', 'LOGOUT', 'CREATE', 'UPDATE', 'DELETE'
  entityType: String, // 'USER', 'PRODUCT', 'ORDER'
  entityId: ObjectId,
  changes: {
    before: Object,
    after: Object
  },
  ipAddress: String,
  userAgent: String,
  timestamp: Date,
  metadata: Object
}
```

### Log Retention
- **Audit logs**: 1 year
- **Access logs**: 90 days
- **Error logs**: 30 days
- **Debug logs**: 7 days

---

## Monitoring & Alerting

### Security Metrics to Monitor
- Failed login attempts
- Rate limit violations
- Unusual API usage patterns
- Unauthorized access attempts
- Payment failures
- Data export requests

### Alert Triggers
- **Failed logins**: > 5 per minute per IP
- **Rate limit**: > 90% of limit reached
- **Payment failures**: > 10% failure rate
- **Admin access**: From new location
- **Data export**: Large volume requests

### Monitoring Tools
- **Application monitoring**: New Relic / Datadog
- **Error tracking**: Sentry
- **Log aggregation**: ELK Stack / CloudWatch
- **Security scanning**: OWASP ZAP

---

## Dependency Security

### Package Management
- **Regular updates**: Weekly dependency checks
- **Vulnerability scanning**: npm audit
- **Lock files**: Commit package-lock.json
- **No npm install**: In production builds

### Security Scanning
```bash
# Run security audit
npm audit

# Fix vulnerabilities
npm audit fix

# Check for outdated packages
npm outdated

# Run Snyk security scan
npx snyk test
```

### Trusted Sources
- **npm registry**: Official npm registry only
- **GitHub packages**: For private packages
- **No external scripts**: From untrusted sources

---

## Environment Security

### Environment Variables
- **Never commit**: .env files to git
- **Use .env.example**: Template only
- **Separate configs**: Development, staging, production
- **Secrets management**: AWS Secrets Manager / HashiCorp Vault

### .gitignore
```
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.pem
*.key
credentials.json
```

---

## Database Security

### MongoDB Security
- **Authentication**: Enabled
- **Authorization**: Role-based
- **Network access**: IP whitelisting
- **Encryption at rest**: Enabled
- **Backup encryption**: Enabled

### Connection Security
```javascript
const mongooseOptions = {
  useNewUrlParser: true,
  useUnifiedTopology: true,
  ssl: true,
  authSource: 'admin',
  auth: {
    username: process.env.MONGODB_USER,
    password: process.env.MONGODB_PASSWORD
  }
};
```

### Query Security
- **No raw queries**: Use Mongoose ODM
- **Input validation**: Before queries
- **Projection**: Limit returned fields
- **Indexing**: Prevent NoSQL injection

---

## Redis Security

### Redis Configuration
- **Authentication**: Password protected
- **Network access**: VPC private
- **TLS**: Enabled in production
- **Command restrictions**: Dangerous commands disabled

### Connection Security
```javascript
const redisOptions = {
  host: process.env.REDIS_HOST,
  port: process.env.REDIS_PORT,
  password: process.env.REDIS_PASSWORD,
  tls: process.env.NODE_ENV === 'production' ? {} : undefined
};
```

---

## Payment Security

### Razorpay Integration
- **API keys**: Environment variables
- **Webhook signature**: Verified
- **No card data**: Never stored
- **PCI DSS**: Razorpay compliant

### Payment Flow Security
1. **Client**: Create order with amount
2. **Server**: Generate Razorpay order
3. **Client**: Process payment via Razorpay
4. **Server**: Verify payment signature
5. **Server**: Update order status
6. **Webhook**: Handle payment confirmation

---

## Email Security

### SMTP Configuration
- **TLS**: Enabled
- **Authentication**: Required
- **Rate limiting**: Per provider limits
- **SPF/DKIM**: Configured

### Email Content
- **No sensitive data**: In email body
- **Verification links**: Time-limited tokens
- **Unsubscribe**: Required for marketing emails
- **Plain text**: Alternative to HTML

---

## GDPR Compliance

### Data Protection
- **Data minimization**: Collect only necessary data
- **Data retention**: Automatic deletion after retention period
- **Right to deletion**: User can delete account
- **Data portability**: Export user data on request

### Consent Management
- **Explicit consent**: For data collection
- **Cookie consent**: Banner on first visit
- **Marketing opt-in**: Separate consent
- **Privacy policy**: Clear and accessible

### Data Breach Response
- **Detection**: Within 24 hours
- **Notification**: Within 72 hours
- **Mitigation**: Immediate action
- **Documentation**: Detailed incident report

---

## OWASP Top 10 Mitigation

### A01:2021 – Broken Access Control
- **Implementation**: RBAC, ownership checks
- **Verification**: Regular access audits

### A02:2021 – Cryptographic Failures
- **Implementation**: TLS, bcrypt, encryption at rest
- **Verification**: Regular security scans

### A03:2021 – Injection
- **Implementation**: Parameterized queries, input validation
- **Verification**: Penetration testing

### A04:2021 – Insecure Design
- **Implementation**: Threat modeling, security by design
- **Verification**: Security architecture review

### A05:2021 – Security Misconfiguration
- **Implementation**: Hardened configurations, security headers
- **Verification**: Configuration audits

### A06:2021 – Vulnerable Components
- **Implementation**: Dependency scanning, regular updates
- **Verification**: Automated vulnerability scanning

### A07:2021 – Identification and Authentication Failures
- **Implementation**: MFA ready, strong password policy
- **Verification**: Authentication testing

### A08:2021 – Software and Data Integrity Failures
- **Implementation**: CI/CD security, code signing
- **Verification**: Supply chain security

### A09:2021 – Security Logging and Monitoring Failures
- **Implementation**: Comprehensive logging, monitoring
- **Verification**: Log analysis, alert testing

### A10:2021 – Server-Side Request Forgery (SSRF)
- **Implementation**: Input validation, allowlist
- **Verification**: SSRF testing

---

## Security Testing

### Automated Testing
- **Unit tests**: Security-focused unit tests
- **Integration tests**: Security flows
- **Dependency scanning**: npm audit, Snyk
- **SAST**: Static application security testing

### Manual Testing
- **Penetration testing**: Quarterly
- **Security review**: Before major releases
- **Code review**: Security-focused review
- **Threat modeling**: For new features

### Security Checklist
- [ ] All endpoints authenticated where required
- [ ] Input validation on all inputs
- [ ] Output encoding for user-generated content
- [ ] SQL injection testing
- [ ] XSS testing
- [ ] CSRF testing
- [ ] Rate limiting configured
- [ ] Security headers configured
- [ ] HTTPS enforced
- [ ] Password hashing verified
- [ ] JWT token security verified
- [ ] File upload security verified
- [ ] Dependency vulnerabilities fixed
- [ ] Audit logging enabled
- [ ] Error handling secure
- [ ] Environment variables secured
- [ ] Database access secured
- [ ] API keys secured
- [ ] Webhook signature verification
- [ ] Payment security verified

---

## Incident Response Plan

### Incident Categories
1. **Critical**: Data breach, system compromise
2. **High**: Unauthorized access, payment fraud
3. **Medium**: DDoS attack, vulnerability found
4. **Low**: Suspicious activity, minor misconfiguration

### Response Steps
1. **Detection**: Identify incident
2. **Containment**: Limit impact
3. **Eradication**: Remove threat
4. **Recovery**: Restore systems
5. **Lessons Learned**: Document and improve

### Response Team
- **Security Lead**: Incident commander
- **DevOps**: System recovery
- **Development**: Code fixes
- **Legal**: Compliance and notification
- **PR**: Communication

---

## Security Best Practices

### Development
- **Code review**: Security-focused review
- **Secure coding**: Follow OWASP guidelines
- **Least privilege**: Minimal permissions
- **Defense in depth**: Multiple security layers

### Deployment
- **Secure pipelines**: CI/CD security
- **Environment separation**: Dev, staging, production
- **Secrets management**: Proper secret storage
- **Monitoring**: Comprehensive logging

### Operations
- **Regular updates**: Security patches
- **Backup testing**: Regular restore tests
- **Access reviews**: Periodic access audits
- **Security training**: Team awareness

---

## Conclusion

This security design provides:
- **Comprehensive Protection**: Multi-layered security
- **OWASP Compliance**: Top 10 mitigations
- **GDPR Ready**: Data protection measures
- **Production-Grade**: Enterprise-level security
- **Monitoring**: Continuous security monitoring
- **Incident Response**: Structured response plan
- **Best Practices**: Industry-standard security measures
- **AI-Ready**: Security foundation for future AI features
